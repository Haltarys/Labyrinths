# frozen_string_literal: true

RSpec.describe Labyrinths do
  it "has a version number" do
    expect(Labyrinths::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
